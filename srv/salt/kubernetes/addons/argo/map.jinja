# -*- coding: utf-8 -*-
# vim: ft=jinja

{% import_yaml tpldir ~ "/defaults.yaml" or {} as defaults %}

{#- Merge in salt pillar #}
{% set argo = salt['pillar.get']('kubernetes:charts:argo', default=defaults['argo'], merge=True) %}