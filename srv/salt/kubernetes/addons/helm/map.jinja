# -*- coding: utf-8 -*-
# vim: ft=jinja

{% import_yaml tpldir ~ "/defaults.yaml" or {} as defaults %}

{#- Merge in salt pillar #}
{% set helm = salt['pillar.get']('kubernetes:common:addons:helm', default=defaults['helm'], merge=True) %}