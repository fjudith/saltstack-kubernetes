# -*- coding: utf-8 -*-
# vim: ft=jinja

{% import_yaml tpldir ~ "/defaults.yaml" or {} as defaults %}

{#- Merge in salt pillar #}
{% set httpbin = salt['pillar.get']('kubernetes:common:addons:httpbin', default=defaults['httpbin'], merge=True) %}