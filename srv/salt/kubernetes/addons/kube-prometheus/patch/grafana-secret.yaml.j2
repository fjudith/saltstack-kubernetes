# -*- coding: utf-8 -*-
# vim: ft=jinja

{#- Get the `tplroot` from `tpldir` #}
{% from tpldir ~ "/map.jinja" import kube_prometheus with context %}

apiVersion: v1
kind: Secret
metadata:
  name: grafana-secret
  namespace: monitoring
  labels:
    component: grafana
type: Opaque
data:
  admin-password: {{ kube_prometheus.grafana_password | base64_encode }}