# -*- coding: utf-8 -*-
# vim: ft=jinja

{% import_yaml tpldir ~ "/defaults.yaml" or {} as defaults %}

{#- Merge in salt pillar #}
{% set kube_scan = salt['pillar.get']('kubernetes:common:addons:kube_scan', default=defaults['kube_scan'], merge=True) %}