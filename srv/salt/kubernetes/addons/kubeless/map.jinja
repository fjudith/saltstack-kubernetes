# -*- coding: utf-8 -*-
# vim: ft=jinja

{% import_yaml tpldir ~ "/defaults.yaml" or {} as defaults %}

{#- Merge in salt pillar #}
{% set kubeless = salt['pillar.get']('kubernetes:common:addons:kubeless', default=defaults['kubeless'], merge=True) %}