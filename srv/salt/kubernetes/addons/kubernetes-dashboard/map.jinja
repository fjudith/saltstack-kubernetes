# -*- coding: utf-8 -*-
# vim: ft=jinja

{% import_yaml tpldir ~ "/defaults.yaml" or {} as defaults %}

{#- Merge in salt pillar #}
{% set dashboard = salt['pillar.get']('kubernetes:common:addons:dashboard', default=defaults['dashboard'], merge=True) %}