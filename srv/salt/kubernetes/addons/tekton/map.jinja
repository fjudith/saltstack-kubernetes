# -*- coding: utf-8 -*-
# vim: ft=jinja

{% import_yaml tpldir ~ "/defaults.yaml" or {} as defaults %}

{#- Merge in salt pillar #}
{% set tekton = salt['pillar.get']('kubernetes:common:addons:tekton', default=defaults['tekton'], merge=True) %}