# -*- coding: utf-8 -*-
# vim: ft=jinja

{% import_yaml tpldir ~ "/defaults.yaml" or {} as defaults %}

{#- Merge in salt pillar #}
{% set argo_events = salt['pillar.get']('kubernetes:charts:argo_events', default=defaults['argo_events'], merge=True) %}