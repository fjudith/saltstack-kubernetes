# -*- coding: utf-8 -*-
# vim: ft=jinja

{% import_yaml tpldir ~ "/defaults.yaml" or {} as defaults %}

{#- Merge in salt pillar #}
{% set falco = salt['pillar.get']('kubernetes:charts:falco', default=defaults['falco'], merge=True) %}