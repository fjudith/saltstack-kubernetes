# -*- coding: utf-8 -*-
# vim: ft=jinja

{% import_yaml tpldir ~ "/defaults.yaml" or {} as defaults %}

{#- Merge in salt pillar #}
{% set fission = salt['pillar.get']('kubernetes:charts:fission', default=defaults['fission'], merge=True) %}