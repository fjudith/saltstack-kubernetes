# -*- coding: utf-8 -*-
# vim: ft=jinja

{% import_yaml tpldir ~ "/defaults.yaml" or {} as defaults %}

{#- Merge in salt pillar #}
{% set gitea = salt['pillar.get']('kubernetes:charts:gitea', default=defaults['gitea'], merge=True) %}