# -*- coding: utf-8 -*-
# vim: ft=jinja

{% import_yaml tpldir ~ "/defaults.yaml" or {} as defaults %}

{#- Merge in salt pillar #}
{% set harbor = salt['pillar.get']('kubernetes:charts:harbor', default=defaults['harbor'], merge=True) %}