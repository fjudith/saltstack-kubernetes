# -*- coding: utf-8 -*-
# vim: ft=jinja

{% import_yaml tpldir ~ "/defaults.yaml" or {} as defaults %}

{#- Merge in salt pillar #}
{% set jenkins = salt['pillar.get']('kubernetes:charts:jenkins', default=defaults['jenkins'], merge=True) %}