# -*- coding: utf-8 -*-
# vim: ft=jinja

{% import_yaml tpldir ~ "/defaults.yaml" or {} as defaults %}

{#- Merge in salt pillar #}
{% set keycloak_gatekeeper = salt['pillar.get']('kubernetes:charts:keycloak_gatekeeper', default=defaults['keycloak_gatekeeper'], merge=True) %}