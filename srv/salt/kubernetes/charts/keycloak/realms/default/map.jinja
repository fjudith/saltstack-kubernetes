# -*- coding: utf-8 -*-
# vim: ft=jinja

{% import_yaml tpldir ~ "/../../defaults.yaml" or {} as defaults %}

{#- Merge in salt pillar #}
{% set keycloak = salt['pillar.get']('kubernetes:charts:keycloak', default=defaults['keycloak'], merge=True) %}