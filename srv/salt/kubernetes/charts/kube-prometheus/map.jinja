# -*- coding: utf-8 -*-
# vim: ft=jinja

{% import_yaml tpldir ~ "/defaults.yaml" or {} as defaults %}

{#- Merge in salt pillar #}
{% set kube_prometheus = salt['pillar.get']('kubernetes:charts:kube_prometheus', default=defaults['kube_prometheus'], merge=True) %}