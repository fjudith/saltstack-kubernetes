# -*- coding: utf-8 -*-
# vim: ft=jinja

{% import_yaml tpldir ~ "/defaults.yaml" or {} as defaults %}

{#- Merge in salt pillar #}
{% set mailhog = salt['pillar.get']('kubernetes:charts:mailhog', default=defaults['mailhog'], merge=True) %}