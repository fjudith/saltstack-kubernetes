# -*- coding: utf-8 -*-
# vim: ft=jinja

{% import_yaml tpldir ~ "/defaults.yaml" or {} as defaults %}

{#- Merge in salt pillar #}
{% set nuclio = salt['pillar.get']('kubernetes:charts:nuclio', default=defaults['nuclio'], merge=True) %}