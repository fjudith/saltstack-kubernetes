# -*- coding: utf-8 -*-
# vim: ft=jinja

{% import_yaml tpldir ~ "/defaults.yaml" or {} as defaults %}

{#- Merge in salt pillar #}
{% set proxyinjector = salt['pillar.get']('kubernetes:charts:proxyinjector', default=defaults['proxyinjector'], merge=True) %}