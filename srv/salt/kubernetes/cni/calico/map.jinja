# -*- coding: utf-8 -*-
# vim: ft=jinja

{% import_yaml tpldir ~ "/defaults.yaml" or {} as defaults %}

{#- Merge in salt pillar #}
{% set calico = salt['pillar.get']('kubernetes:common:cni:calico', default=defaults['calico'], merge=True) %}