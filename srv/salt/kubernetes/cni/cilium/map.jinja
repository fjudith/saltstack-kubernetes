# -*- coding: utf-8 -*-
# vim: ft=jinja

{% import_yaml tpldir ~ "/defaults.yaml" or {} as defaults %}

{#- Merge in salt pillar #}
{% set cilium = salt['pillar.get']('kubernetes:common:cni:cilium', default=defaults['cilium'], merge=True) %}