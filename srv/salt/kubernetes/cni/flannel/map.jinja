# -*- coding: utf-8 -*-
# vim: ft=jinja

{% import_yaml tpldir ~ "/defaults.yaml" or {} as defaults %}

{#- Merge in salt pillar #}
{% set flannel = salt['pillar.get']('kubernetes:common:cni:flannel', default=defaults['flannel'], merge=True) %}