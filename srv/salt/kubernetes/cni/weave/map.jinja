# -*- coding: utf-8 -*-
# vim: ft=jinja

{% import_yaml tpldir ~ "/defaults.yaml" or {} as defaults %}

{#- Merge in salt pillar #}
{% set weave = salt['pillar.get']('kubernetes:common:cni:weave', default=defaults['weave'], merge=True) %}