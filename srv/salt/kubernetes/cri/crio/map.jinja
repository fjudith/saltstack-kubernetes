# -*- coding: utf-8 -*-
# vim: ft=jinja

{% import_yaml tpldir ~ "/defaults.yaml" or {} as defaults %}

{#- Merge in salt pillar #}
{% set containerd = salt['pillar.get']('kubernetes:common:cri:crio', default=defaults['containerd'], merge=True) %}