# -*- coding: utf-8 -*-
# vim: ft=jinja

{% import_yaml tpldir ~ "/defaults.yaml" or {} as defaults %}

{#- Merge in salt pillar #}
{% set longhorn = salt['pillar.get']('kubernetes:storage:longhorn', default=defaults['longhorn'], merge=True) %}