# -*- coding: utf-8 -*-
# vim: ft=jinja

{% from tpldir ~ "/map.jinja" import rook_ceph with context %}
{%- from "kubernetes/map.jinja" import common with context -%}

image:
  tag: v{{ rook_ceph.version }}

monitoring:
  {%- if common.addons.get('kube_prometheus', {'enabled': False}).enabled %}
  enabled: true
  {%- endif %}
