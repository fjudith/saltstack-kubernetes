# -*- coding: utf-8 -*-
# vim: ft=jinja

{% import_yaml tpldir ~ "/defaults.yaml" or {} as defaults %}

{#- Merge in salt pillar #}
{% set cert_manager = salt['pillar.get']('kubernetes:common:addons:cert_manager', default=defaults['cert_manager'], merge=True) %}