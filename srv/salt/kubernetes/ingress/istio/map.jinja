# -*- coding: utf-8 -*-
# vim: ft=jinja

{% import_yaml tpldir ~ "/defaults.yaml" or {} as defaults %}

{#- Merge in salt pillar #}
{% set istio = salt['pillar.get']('kubernetes:common:addons:istio', default=defaults['istio'], merge=True) %}