{%- set public_domain = pillar['public-domain'] -%}
{%- from "kubernetes/map.jinja" import common with context -%}

apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: istio-jaeger
  namespace: default
spec:
  gateways:
  - istio-gateway
  hosts:
  - istio-tracing.{{ public_domain }}
  http:
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        host: jaeger-query.istio-system.svc.cluster.local
        port:
          number: 16686
---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: istio-servicegraph
  namespace: default
spec:
  gateways:
  - istio-gateway
  hosts:
  - istio-servicegraph.{{ public_domain }}
  http:
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        host: servicegraph.istio-system.svc.cluster.local
        port:
          number: 8088
---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: istio-telemetry
  namespace: default
spec:
  gateways:
  - istio-gateway
  hosts:
  - istio-telemetry.{{ public_domain }}
  http:
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        host: istio-telemetry.istio-system.svc.cluster.local
        port:
          number: 9093
---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: istio-prometheus
  namespace: default
spec:
  gateways:
  - istio-gateway
  hosts:
  - istio-prometheus.{{ public_domain }}
  http:
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        host: prometheus.istio-system.svc.cluster.local
        port:
          number: 9090
---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: istio-grafana
  namespace: default
spec:
  gateways:
  - istio-gateway
  hosts:
  - istio-grafana.{{ public_domain }}
  http:
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        host: grafana.istio-system.svc.cluster.local
        port:
          number: 3000
---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: istio-kiali
  namespace: default
spec:
  gateways:
  - istio-gateway
  hosts:
  - istio-kiali.{{ public_domain }}
  http:
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        host: kiali.istio-system.svc.cluster.local
        port:
          number: 20001
---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: bookinfo
  namespace: default
spec:
  gateways:
  - istio-gateway
  hosts:
  - bookinfo.{{ public_domain }}
  http:
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        host: productpage.bookinfo.svc.cluster.local
        port:
          number: 9080