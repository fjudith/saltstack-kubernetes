# -*- coding: utf-8 -*-
# vim: ft=jinja

{% import_yaml tpldir ~ "/defaults.yaml" or {} as defaults %}

{#- Merge in salt pillar #}
{% set nginx = salt['pillar.get']('kubernetes:common:addons:nginx', default=defaults['nginx'], merge=True) %}