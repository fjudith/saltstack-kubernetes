# -*- coding: utf-8 -*-
# vim: ft=jinja

{% import_yaml tpldir ~ "/defaults.yaml" or {} as defaults %}

{#- Merge in salt pillar #}
{% set loopback_iscsi = salt['pillar.get']('kubernetes:storage:loopback_iscsi', default=defaults['loopback_iscsi'], merge=True) %}